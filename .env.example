# DashScope (Alibaba Qwen) API Key — required for AI extraction
DASHSCOPE_API_KEY=sk-...

# Optional: Anthropic API Key for Claude vision fallback
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Use local OCR only (no API cost, less accurate)
# USE_LOCAL_OCR_ONLY=false
